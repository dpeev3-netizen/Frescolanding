
  # Add Cool Animations

  This is a code bundle for Add Cool Animations. The original project is available at https://www.figma.com/design/MO83tWCYyYJJVATePqzJdD/Add-Cool-Animations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  